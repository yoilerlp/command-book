using System.Collections.Generic;
using Comandos.Models;

namespace Comandos.Data {

    /*
    public class MockCommanderRepo : ICommanderRepo
    {
        public IEnumerable<Command> GetAllCommands()
        {
            var commands = new List<Command>(){
                new Command{CommandId=1, HowTo="Clear Up", Line="home clear()", Platform="House And office" },
                new Command{CommandId=2, HowTo="Wake Up", Line="please wake up", Platform="room" },
                new Command{CommandId=3, HowTo="cook", Line="cook something", Platform="House" }
            };
            return commands;
        }

        public Command GetCommandById(int id)
        {
            return new Command{CommandId=id, HowTo="Clear Up", Line="home clear()", Platform="House And office" };
        }
    }
    */
}